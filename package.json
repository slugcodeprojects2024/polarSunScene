{
  "name": "terror-survival-game",
  "version": "1.0.0",
  "description": "Arctic survival horror game inspired by The Terror",
  "main": "index.html",
  "scripts": {
    "start": "parcel src/index.html --open",
    "build": "parcel build src/index.html --dist-dir docs --public-url ./",
    "deploy": "npm run build && git add docs && git commit -m 'Deploy to GitHub Pages' && git push"
  },
  "dependencies": {
    "three": "^0.160.0",
    "cannon-es": "^0.20.0",
    "stats.js": "^0.17.0"
  },
  "devDependencies": {
    "parcel": "^2.10.0",
    "@parcel/transformer-glsl": "^2.10.0"
  }
}
